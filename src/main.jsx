import React from "react";
import ReactDOM from "react-dom/client";
import TrainingLogApp from "./training_log_app";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrainingLogApp />
  </React.StrictMode>
);
